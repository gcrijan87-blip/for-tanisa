<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Tanisha</title>
    <style>
        body {
            background-color: #fff0f3;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            font-family: 'Georgia', serif;
            overflow: hidden;
            transition: background-color 2s ease;
        }

        .gallery { 
            display: flex; 
            align-items: center; 
            gap: 25px; 
            margin-bottom: 40px; 
        }

        .photo {
            background: white;
            padding: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
            animation: float 5s ease-in-out infinite;
            border-radius: 10px;
        }

        /* THICK BORDERS FOR TANISHA */
        .pic1 { 
            width: 170px; 
            border: 12px solid #ff4d6d; 
            transform: rotate(-5deg); 
        }
        .pic2 { 
            width: 150px; 
            border: 10px solid #c9184a; 
            transform: rotate(5deg); 
            animation-delay: 2.5s; 
        }

        img { 
            width: 100%; 
            height: auto; 
            display: block; 
            border-radius: 5px; 
            object-fit: cover;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(-5deg); }
            50% { transform: translateY(-20px) rotate(-2deg); }
        }

        #bouquet { 
            font-size: 100px; 
            cursor: pointer; 
            transition: 0.3s; 
            z-index: 10; 
            user-select: none;
            -webkit-tap-highlight-color: transparent; 
        }

        #message {
            font-size: 2.2rem;
            text-align: center;
            margin-top: 25px;
            opacity: 0;
            color: #c9184a;
            transition: 2s ease;
            transform: scale(0.8);
            font-weight: bold;
        }

        .heart { position: fixed; bottom: -50px; pointer-events: none; animation: rise 4s linear forwards; }
        @keyframes rise { to { transform: translateY(-110vh) rotate(360deg); opacity: 0; } }
    </style>
</head>
<body>

    <div class="gallery">
        <div class="photo pic1"><img src="1.jpg" alt="Us"></div>
        <div class="photo pic2"><img src="2.jpg" alt="Tanisha"></div>
    </div>

    <div id="bouquet" onclick="startSurprise()">💐</div>
    <h1 id="message">For you, Tanisha... ❤️</h1>

    <script>
        function startSurprise() {
            document.body.style.backgroundColor = "#ffccd5";
            document.getElementById('bouquet').innerHTML = '🌹';
            document.getElementById('message').style.opacity = '1';
            document.getElementById('message').style.transform = 'scale(1)';

            setInterval(() => {
                const h = document.createElement('div');
                h.className = 'heart';
                h.innerHTML = '❤️';
                h.style.left = Math.random() * 100 + 'vw';
                h.style.fontSize = (Math.random() * 20 + 15) + 'px';
                document.body.appendChild(h);
                setTimeout(() => h.remove(), 4000);
            }, 300);
            document.getElementById('bouquet').onclick = null;
        }
    </script>
</body>
</html>
